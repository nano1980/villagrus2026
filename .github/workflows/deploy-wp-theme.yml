name: Deploy WP Theme to one.com

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Bekräfta deploy till live (skriv "ja")'
        required: true
        default: ''

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Kräver manuellt godkännande
        if: ${{ github.event.inputs.confirm != 'ja' }}
        run: echo "Deploy avbruten — skriv 'ja' i bekräftelse-fältet." && exit 1

      - uses: actions/checkout@v4

      - name: Install sshpass
        run: sudo apt-get install -y sshpass

      - name: Deploy theme via rsync
        env:
          SSHPASS: ${{ secrets.SFTP_PASSWORD }}
        run: |
          sshpass -e rsync -rltz \
            -e "ssh -p 22 -o StrictHostKeyChecking=no" \
            --exclude='.DS_Store' \
            --exclude='*.map' \
            wp-theme/villagrus/ \
            "hernangil.com@ssh.hernangil.com:/customers/d/6/9/hernangil.com/httpd.www/vg2026/wp-content/themes/villagrus/" \
          || RSYNC_EXIT=$? && [ "${RSYNC_EXIT:-0}" -eq 23 ] && echo "Filer överförda (exit 23 = checksums-varning på one.com, ej kritiskt)" || exit $RSYNC_EXIT
